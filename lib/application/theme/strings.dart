const height = 'HEIGHT';

const cm = 'cm';

const age = 'AGE';

const weight = 'WEIGHT';

const result = 'RESULT';

const bmiResultTitle = 'BMI RESULT';

const calculate = 'RE-CALCULATE';

const male = 'Male';

const female = 'Female';

const bmiCalculatorTitle = 'BMI CALCULATOR';

const bmiResult = 'BMI RESULT';

const resultTitle = 'YOUR RESULT';
